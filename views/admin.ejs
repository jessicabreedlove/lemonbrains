<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin | LemonBrains</title>
    <link rel="stylesheet" href="/css/style.css">
    <script type="text/javascript" src="/js/script.js"></script>
</head>
<body>
    <!-- 
        Admin page
    -->
    <h1>Admin</h1>
    <br>Need to know how location will be used on the frontend e.g. can css just be replaced?  
    <br>Or can a value be set to show a specific part of the html.  In anycase, this is the page to provide admin options
    <ul>
        <li>TODO: Query locations collections, show all locations</li>
        <li>TODO: Upload new locations</li>
        <li>TODO: Delete locations</li>
        <li>TODO: Update existing locations</li>
        <li>PUT: Ability to toggle admin role is possible via PUT or simulated GET /admin/role will act on current user</li>
        <li>DELETE: Ability to delete user, stand, stats is possible via DELETE or simulated GET /admin/delete will act on current user</li>
        <li>What else should be here?</li>
    </ul>

    <% if (locals.role && role) { %>
        <h2>The admin role for <%= result.authid %> has been changed to: <%= admin %></h2>
    <% } %>


    <% if (locals.delete) { %>
        <h2>The user with authid <%= result.authid %> has been deleted</h2>
    <% } %>

    <a href="/api-docs">Swagger documentation</a>
</body>
</html>